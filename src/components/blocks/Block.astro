---
interface Props {
	class?: string;
	[key: string]: any;
}

const { class: className, ...rest } = Astro.props;
---

<div class:list={["h-fit w-full md:h-full md:w-fit", className]} {...rest}>
	<slot />
</div>
